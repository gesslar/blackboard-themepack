name: Quality

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  Quality:
    uses: gesslar/Maint/.github/workflows/QualityTheme.yaml@main
    secrets: inherit
    with:
      theme_files: "src/*.sassy.yaml"
      strict: "no"
